import { DynamicDropdownDirective } from './dynamic-dropdown.directive';

describe('DynamicDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
